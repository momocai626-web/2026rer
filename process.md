# 開發歷程記錄

---

### 22:50 - 系統優化成果報告整合
- **變更總覽**：將「復健紀錄管理系統」從基礎的 MVP 升級為具備專業醫療感、日常親切實用性以及衛教宣導能力的高保真臨床應用系統。優化涵蓋了視覺設計、互動體驗、響應式閱讀、衛教整合以及後端邏輯。
- **重點更新內容**：
  1. **醫療專業級視覺與極簡動畫**：引入以「醫療綠 (Teal 600)」為核心的色系，搭配 Bootstrap 5.3 Icons 元件，全面取代 legacy emojis；採用 `huashu-design` 推薦的頂級 `cubic-bezier(0.16, 1, 0.3, 1)` 緩動曲線實作 `fadeUp` 進場動畫；將免責聲明、隱私權宣告等 Modal 更換為彈簧阻尼 `cubic-bezier(0.34, 1.56, 0.64, 1)` 動畫，搭配極致尊榮的 `backdrop-filter: blur(6px)` 背景毛玻璃景深。
  2. **首頁 RWD 閱讀無障礙調優**：使用 `<span class="text-nowrap">` 保護 `(ROM)` 與 `(VAS)` 醫學縮寫；手機端段落套用 `text-align: justify; text-justify: inter-ideograph;`；自適應縮放手機端主標題至宜讀的 `1.6rem`，並設定行高為舒服的 `1.75`。
  3. **衛教資訊單元與原創海報燈箱**：於首頁置入「衛教資訊」區塊（卡片式衛教網格）；對接 ChatGPT Image 2.0 海報（三張超高品質的原創繁體中文醫療海報：`poster-exercise.png`、`poster-tracker.png`、`poster-pain.png`）；實作大圖燈箱 Modal；整合 `onerror="this.parentNode.classList.add('img-placeholder')"` 無損圖片降級機制。
  4. **文案降溫，還原每日記錄本質 (Anti-Slop)**：移除過度浮誇的詞語；修正為「**每日復健紀錄系統**」與「**個案每日紀錄回報**」。
- **修改檔案清單**：更新了 index.html、style.css、images/、plan.md、process.md、notebook.md。

---

### 22:55 - 開發修練心法整合
- **實用本質的歸位 (Anti-Slop & Practicality)**：身為高級軟體工程師，有時容易沉浸於高階學術詞彙與浮誇行銷中（如「決策鏈、數據驅動康復體系」），卻忽略了真實使用者往往只是需要一個簡單親切、像日記本一樣的「每日記錄器」。與真實臨床使用者對齊，適時將文案「降溫」，是極佳的專業素養與職業操守。
- **衛教海報無縫對接與燈箱設計 (Infographics Integration)**：將專業的衛教海報融入系統時，有兩大核心設計心法：
  1. **無損降級設計**：網頁載入大圖有時會失敗或延遲，使用 `onerror="this.parentNode.classList.add('img-placeholder')"` 觸發純 CSS 輕背景佔位框「海報即將上架」，能讓系統隨時保持得體且不崩潰的優雅姿態。
  2. **高畫質燈箱機制 (Lightbox Modal)**：衛教海報文字精細，一般網頁卡片難以閱讀。必須實作點擊原尺寸燈箱展示、背景毛玻璃模糊、以及支援滑鼠點擊外緣或 `Escape` 鍵盤迅速關閉的極簡互動，提供病患最體貼的閱讀體驗。
- **Agent Skills 架構思維**：在設計與調整 UI 時，區分上帝視角 (Admin) 與使用者視角 (Client) 是保持介面清爽的關鍵。如同今天移除多餘的技術支援連結與首頁導覽卡片，有助於讓使用者專注於核心任務（登入與填寫紀錄）。
- **響應式無障礙閱讀力學 (RWD Reading Accessibility)**：在手機端實現高品質的中英混排閱讀，關鍵在於三點：一是利用 `<span class="text-nowrap">` 保護醫學縮寫（如 `(ROM)`、`(VAS)` 等），防止英文與括號被從中截斷，維護學術尊嚴；二是使用 `text-align: justify; text-justify: inter-ideograph;`，徹底消除手機窄屏下中英文混排常見的右側不規則「邊緣鋸齒（Ragged Edges）」；三是實現字型的動態流體自適應（如手機端從 `2.5rem` 收縮至 `1.6rem`），並保留充足的行高（如 `1.75`），這才是真正的無障礙閱讀。
- **高級 Modal 動態力學 (Modal Kinetics)**：提升彈出視窗（Modal）質感的兩大核心手段：一是採用彈簧阻尼（Spring Back-Out）曲線（如 `cubic-bezier(0.34, 1.56, 0.64, 1)`），讓卡片在彈出時有輕微的果凍回彈力學，顯得生動精緻；二是運用背景毛玻璃模糊（`backdrop-filter: blur(6px)`）形成深度的視覺景深（Depth of Field），讓使用者的注意力高度聚焦在當前彈窗，同時感受極致的高階 iOS 質感。
- **更正科室為「復健部」**：臨床系統對科室名稱的精確性極為敏感。將「物理治療室」更正為更高等級且正式的「復健部」（Department of Rehabilitation），能更好覆蓋包含物理治療、職能治療等多元業務，也展現了軟體開發與真實行政層級高度對齊的嚴謹態度。
- **精準科室客製化 (Niche Customization)**：軟體系統的生命力在於跟真實場景無縫接軌。明確將品牌標記為「臺大醫院雲林分院 斗六院區 物理治療室」，相比籠統的「復健部」或「醫療系統」，能給予現場物理治療師與病患極強的歸屬感與落地感。這是一切高品質臨床軟體開發的核心心法。
- **精準專業文案 (Clinical Terminology)**：優質的醫療系統不該使用空泛的行銷話術（如「數據驅動的康復體驗」）。應直接使用領域核心術語，如**關節活動度 (ROM)**、**疼痛評分 (VAS)**、**運動順從性 (Compliance)**。結合使用者實際的工作環境（如臺大醫院雲林分院斗六院區）進行語境深度客製化，能讓系統專業度與可信度瞬間躍升。
- **克制即是高級 (Restraint in Motion)**：在實用型工具 (如醫療系統) 中，動畫不該喧賓奪主。放棄複雜的時間軸與過度活潑的效果，改用純 CSS 配合頂級物理緩動曲線 (`cubic-bezier(0.16, 1, 0.3, 1)`) 的 `onLoad` 進場，並且加入 `sessionStorage` 限制只播一次，這才是尊重使用者的好設計。
- **漸進式優化**：不需一次到位，透過用戶即時回饋（如：移除特定元件）進行小步快跑的迭代，是保持專案靈活度的最佳實踐。

---

### 22:23 - ChatGPT Image 2.0 衛教海報生成與無縫對接
- **專業衛教海報設計**：使用 ChatGPT Image 2.0 精準生成三張繁體中文衛教海報，分別是：
  1. `poster-exercise.png` (五十肩居家復健運動指南 - 爬牆、毛巾操、鐘擺)
  2. `poster-tracker.png` (一週居家復健追蹤表)
  3. `poster-pain.png` (疼痛程度評估說明)
- **實地圖片導入**：圖片已成功對齊並複製入 `d:\AI\斗六台大\202606\images\`，首頁圖片路徑更新，提供個案最高質感的居家復健對照範本。

### 22:20 - 新增「衛教資訊」區塊與海報展示燈箱
- **卡片式網格排版**：於首頁 (`index.html`) footer 前方置入三張卡片，對應不同的衛教資訊主題。Hover 卡片時觸發微縮放動效與高階綠色遮罩，展現 🔍 點擊放大按鈕。
- **燈箱放大功能**：實作 `openPosterModal` 與 `closePosterModal` JS 機制，點擊海報即可開啟毛玻璃大圖燈箱，同時支援 `Escape` 鍵與點擊外部區域關閉，使用者體驗極佳。
- **無損圖片降級**：新增 `onerror="this.parentNode.classList.add('img-placeholder')"` 降級樣式，若圖片載入失敗會自動顯示「海報即將上架」的輕柔背景，確保網頁無死角。

### 22:15 - 精簡文案降溫與還原系統實用本質
- **回歸實用復健本質**：移除了先前過於 academic 或商業化的誇大措辭（如「生理回饋、大數據決策鏈、ROM 決策系統」）。
- **親切與在地化語境**：將標題改為溫和的「**每日復健紀錄系統**」與「**個案每日紀錄回報**」，點出專案旨在記錄個案復健過程，減少文字浮誇感，使整體介面更加專業、平實且充滿人文關懷。

### 21:50 - 首頁文字 RWD 閱讀無障礙調優與字體響應式設計
- **專有名詞 nowrap 保護**：在首頁 HTML 中，使用 `<span class="text-nowrap">` 保護關鍵縮寫 `(ROM)` 與 `(VAS)`，保證無論螢幕寬度再窄，縮寫字與括號都不會被從中拆斷折行，維護醫學術語的嚴謹與可讀性。
- **手機端排版兩端對齊 (Justify)**：在小於 640px 的行動裝置上，對 Hero 區塊的說明文字套用 `text-align: justify; text-justify: inter-ideograph;`，完全消除中文、英文和括號混排時右側形成的不規則鋸齒，呈現雜誌排版般的高度對稱美。
- **自適應字型縮放**：為 Hero 標題、說明文字與徽章量身設計了響應式尺寸。手機端的主標題自動從 `2.5rem` 縮放至最宜讀的 `1.6rem`，段落行高設定為舒適的 `1.75`，完全杜絕小螢幕的閱讀壓迫感。
- **實地行動端模擬測試**：透過瀏覽器測試工具，在 375px 寬度的 iPhone 規格下進行實地渲染驗證，排版、圖片以及排版折行都達到極致完美，已錄製動態效果與截圖至 `walkthrough.md`.

### 21:42 - Modal 彈出阻尼動畫與毛玻璃背景景深優化
- **Q彈阻尼彈出動畫**：將全站 Modal 中央卡片更換為高規格的彈簧阻尼（Spring Back-Out）緩動曲線 `cubic-bezier(0.34, 1.56, 0.64, 1)`，使彈窗呈現帶有微果凍感的回彈效果，極具現代感。
- **毛玻璃景深效果 (Backdrop Blur)**：為 `.modal` 背景遮罩新增 `backdrop-filter: blur(6px)` 與輕量遮罩色，啟動時讓背景產生柔和的毛玻璃磨砂效果，提高使用者專注力與系統的奢華感。
- **多頁面通用提升**：此異動直接優化了首頁 (`index.html`)、登入頁 (`login.html`) 與後台 (`admin.html`) 的所有 Modal 元件，展現了 CSS 架構的模組化優勢。
- **實地瀏覽器測試**：已透過瀏覽器測試工具對首頁的免責聲明進行了實地測試，確認動畫與毛玻璃模糊效果在 Chrome/Edge 中以 60fps 流暢渲染，並自動錄製展示檔存入 `walkthrough.md`.

### 21:39 - 更正科室名稱為「復健部」
- **科室名稱更正**：配合臨床指導，將全站文字從「物理治療室」更正為更具規模、正式的官方科室名稱——**「臺大醫院雲林分院 斗六院區 復健部」**與**「斗六台大醫院 復健部」**。
- **覆蓋檔案**：已同步修正 `index.html`、`admin.html` 與 `rehab-form.html` 的 Title、Header 與 Footer，完美達到官方臨床水準。

### 21:36 - 物理治療室專屬深度深度客製化
- **精準科室客製化**：將系統全站的品牌名稱與頁首、頁尾，統一精準精煉為「臺大醫院雲林分院 斗六院區 物理治療室」與「斗六台大醫院 物理治療室」。
- **覆蓋檔案**：全面更新了 `index.html`、`admin.html` 與 `rehab-form.html` 內的所有相關標記，讓整個專案完美貼合斗六院區物理治療室的真實臨床運作語境。

### 21:35 - 全面升級文案品質與臨床品牌化
- **臺大醫院品牌置入**：深度分析 workspace 目錄 `斗六台大` 的上下文後，將系統全面客製化為「臺大醫院雲林分院 物理治療與復健生理回饋系統」，增強真實臨床感與落地感。
- **臨床專業術語升級**：將原本通俗或營銷性質的文案（例如「數據驅動的康復體驗」）全面重寫為精準的醫學實證詞彙。包含**關節活動度 (ROM)**、**主觀疼痛指數 (VAS)**、**復健運動順從性 (Compliance)** 與**生理回饋 (Biofeedback)**，涵蓋 `index.html`、`admin.html`、`rehab-form.html` 等核心檔案。

### 21:34 - 導入康復數據與 ROM 主題高質感 SVG 插圖
- **高質感插圖設計**：在首頁設計並置入了一個精美的向量 SVG 插圖，結合「關節活動度 (ROM) 測量」、「伸展力學幾何人體」與「漸變康復數據曲線」，完美呼應物理復健主題。
- **無縫動畫整合**：SVG 元件被編入 `onLoad` 進場時間軸中，並以 `delay-300` (延遲 0.3s) 優雅滑入，讓整體首頁佈局更加飽滿且專業。
- **實地瀏覽器測試**：已透過瀏覽器測試工具對 `http://localhost:5500/index.html?test=1` 進行實地動畫播放，確認視覺效果完美居中且無程式碼報錯。

### 21:28 - 修正為簡約版 onLoad 動畫
- **介面微調**：因先前的獨立動畫頁面過於複雜且不符醫療系統主題，已將 `animation_demo.html` 移除。
- **引入頂級曲線**：改用純 CSS 於首頁 (`index.html`) 加入極簡的 `fadeUp` 進場動畫，並套用 `huashu-design` 推薦的 `cubic-bezier(0.16, 1, 0.3, 1)` 緩動曲線。
- **Session 狀態控制**：加入 `sessionStorage` 判斷，確保進場動畫在同一個瀏覽器 session 期間只會觸發一次，避免重複載入造成視覺干擾。

### 21:13 - 版本重置
- **版本降級**：依據需求將全站對外顯示版本號重設為 `V1.0.0`.

## 2026-05-18 (v1.4 二元化架構版)
### 21:11 - 介面精簡化
- **首頁更新**：依據需求移成了首頁 (`index.html`) 的導覽卡片 (card-grid)，讓首頁呈現更純粹的視覺.

### 21:10 - 微調介面元件
- **Footer 更新**：依據需求移成了首頁 (`index.html`) 頁尾的「技術支援」連結，使介面更加專注於系統本身.

### 21:40 - 介面極簡化與分析整合
- **二元重構**：將三維度架構精簡為「帳號管理」與「數據分析」兩個核心入口，更符合直覺。
- **分析模組整合**：
  - 將「全院平均」與「個人曲線」整合進同一個下拉選單。
  - 當切換至個案分析時，自動動態顯示該員的「紀錄明細表」，達成圖表與文字數據的並行對比。
- **性能優化**：改用 `Promise.all` 同步加載患者與紀錄數據，提升初始化速度.

---

## 開發守則

1. **每次修改 Code.gs 後務必重新部署**
2. **遇到 CORS 錯誤時，優先嘗試將 Content-Type 改為 text/plain**
3. **所有欄位名稱需與 Google Sheet 實際標題完全一致**
4. **重要變更請同步更新本文件與 plan.md**